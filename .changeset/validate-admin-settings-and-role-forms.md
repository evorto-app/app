---
default: patch
---

Reject invalid or missing whole-number organization settings without silently
changing them, normalize regular and platform role writes through one typed
validation path, surface duplicate role names and overview-count failures, render
role and settings validation errors inline, route non-finance platform mutation
failures through typed messages, replace role-selection fan-out with one cached
catalog, reuse parent tax-rate state in the import dialog, keep icon failures
distinct from empty results, stop inactive profile-section queries, and give
icon-only navigation controls accessible names.
