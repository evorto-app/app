Create a new PR on top of codex/pr62-split-06-browser-transport-blocker with update for all dependencies, research all of the changes and migrations that might be needed and [$grill-with-docs](/Users/hedde/.agents/skills/grill-with-docs/SKILL.md) on any decisions that have to be taken

---

continue

---

Can we make sure to also update the vendored sources ?

---

PLEASE IMPLEMENT THIS PLAN:
# Dependency And Vendored Source Refresh PR

## Summary

Create a new stacked branch on top of `origin/codex/pr62-split-06-browser-transport-blocker`, update all root direct dependencies/devDependencies, refresh vendored upstream sources to match the installed Effect and Drizzle versions, apply required migrations, and open a draft PR.

Decisions locked:
- Include Angular 22 and TypeScript 6.
- Preserve prerelease channels: Effect to latest `4.0.0-beta.*`, Drizzle to latest `1.0.0-rc.*`.
- Update vendored `repos/effect/**` and `repos/drizzle/**` to the exact upstream commits/tags matching the installed package versions.
- Update Bun/Docker/CI only where peer/engine validation requires it.

References:
- [Angular version compatibility](https://angular.dev/reference/versions)
- [Angular update guide](https://angular.dev/update-guide)
- [Stripe Node changelog](https://github.com/stripe/stripe-node/blob/master/CHANGELOG.md)
- [Cloudflare TypeScript SDK changelog](https://github.com/cloudflare/cloudflare-typescript/blob/main/CHANGELOG.md)
- [html-to-text changelog](https://github.com/html-to-text/node-html-to-text/blob/master/packages/html-to-text/CHANGELOG.md)

## Key Changes

- Branch setup:
  - Fetch `origin`.
  - Check out the current remote tip of `origin/codex/pr62-split-06-browser-transport-blocker`.
  - Create a Git Town child branch, for example `codex/pr62-dependency-refresh`.

- Dependency update:
  - Update root `package.json` and `bun.lock` for all direct dependencies/devDependencies.
  - Move Angular family, Angular tooling, and Material/CDK to v22-compatible versions.
  - Move TypeScript to v6.0.x.
  - Move Effect packages to latest matching `4.0.0-beta.*`.
  - Move Drizzle packages to latest matching `1.0.0-rc.*`.
  - Update remaining direct packages from `bun outdated`, including Stripe, Cloudflare, Sentry, Tiptap, Playwright, Auth0, Tailwind/PostCSS, ESLint, Prettier, and type packages.
  - Update `packageManager`, Docker image references, Docker Compose, and GitHub Actions Bun setup if engine/peer validation requires a newer Bun than `1.3.11`.

- Vendored source refresh:
  - Resolve installed Effect and Drizzle package versions first.
  - Use `npm view <package>@<version> gitHead repository dist.tarball` to identify the exact upstream source commit or package provenance.
  - Refresh `repos/effect/**` from the upstream Effect monorepo commit matching the installed Effect beta version.
  - Refresh `repos/drizzle/**` from the upstream Drizzle monorepo commit matching the installed Drizzle rc version.
  - If an exact upstream commit cannot be resolved, stop and record the blocker rather than mixing arbitrary upstream `main` with installed package versions.
  - Do not hand-edit vendored source except for snapshot replacement mechanics.

- Migration fixes:
  - Run Angular update tooling where applicable and inspect generated changes.
  - Validate Stripe v22 types around API version, checkout session creation, connected account options, and webhook handling.
  - Validate Cloudflare image direct upload still compiles and returns the expected fields.
  - Validate `html-to-text` and `marked` call sites.
  - Accept lint/format fixes caused by updated ESLint, Prettier, and plugin rules.

## Documentation And Decisions

- Do not create `CONTEXT.md`; dependency and vendored source updates do not introduce product glossary terms.
- Do not create an ADR unless implementation forces a surprising hard-to-reverse decision, such as pinning away from Angular 22 after attempting it.
- Add a Knope/change file only if repo convention or CI requires it.
- Keep PR notes explicit that vendored sources now match the installed Effect/Drizzle versions.

## Test Plan

Run in order:

- `bun install --frozen-lockfile`
- `bun run lint`
- `bun run format:write`
- `bun run build:app`
- `bun run test:unit`
- `bun run test:unit:server`
- Targeted Stripe checkout/webhook tests.
- Targeted Cloudflare image upload tests if present.
- Targeted metadata/migration conversion tests for `html-to-text` and `marked`.
- Docker build/compose validation if Bun image/toolchain changed.
- `bun run test:e2e` after build/unit validation is clean, or document why it could not be completed locally.

## PR Requirements

- Open a draft PR stacked on `codex/pr62-split-06-browser-transport-blocker`.
- PR body should include:
  - dependency groups updated,
  - vendored source versions/commits,
  - major migration notes,
  - runtime/toolchain changes,
  - schema/runtime impact,
  - exact validation commands and results.
- Do not add old-to-new production migration code in this relaunch stack.

---

Can we update bun?

---

Make a note that we have to move all the scripts back to bun eventually

---

Pull in the latest base changes and run the tests only in ci, I need control over local docker atm