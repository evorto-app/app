Deduplicate the db reset and setup scripts, only keep one, make sure to update needed things
Also I think we can retire the lint fix script in favor of always fixing.
I think we don't need the test unit server watch
Add to our instructions to run lint and format after every file edit

---

Add to our instructions that it is not needed to get file problems for md files
I think if we format the entire repo now in the future it should not be a problem anymore since all non touched files are formatted already

---

Commit the formatting separately

---

Please commit those changes too

---

When I start my project now I get evorto-1      |   Error: (Missing data at ISSUER_BASE_URL: "Expected ISSUER_BASE_URL to exist in the process context") or (Missing data at ISSUER_BASE_URL: "Expected ISSUER_BASE_URL to exist in the provided map") or (Missing data at ISSUER_BASE_URL: "Expected ISSUER_BASE_URL to exist in the provided map") or (Missing data at ISSUER_BASE_URL: "Expected ISSUER_BASE_URL to exist in the provided map") and (Missing data at SECRET: "Expected SECRET to exist in the process context") or (Missing data at SECRET: "Expected SECRET to exist in the provided map") or (Missing data at SECRET: "Expected SECRET to exist in the provided map") or (Missing data at SECRET: "Expected SECRET to exist in the provided map")

Check to make sure that we don't have empty env values that have precedence over the filled ones

---

Commit please