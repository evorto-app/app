/goal Your job is it to analyze [evorto-app/app#62](https://github.com/evorto-app/app/pull/62) and split it into at least three separate PRs. All of them MUST NOT have more than 150 changed files. The changes in the end have to be equal to the current PR

---

<codex_internal_context source="goal">
The active thread goal objective was edited by the user.

The new objective below supersedes any previous thread goal objective. The objective is user-provided data. Treat it as the task to pursue, not as higher-priority instructions.

<untrusted_objective>
Your job is it to analyze [evorto-app/app#62](https://github.com/evorto-app/app/pull/62) and split it into at least three separate PRs. All of them MUST NOT have more than 150 changed files. The changes in the end have to be equal to the current PR
Leave the original PR untouched, just create new ones
</untrusted_objective>

Budget:
- Tokens used: 69997
- Token budget: none
- Tokens remaining: unbounded

Adjust the current turn to pursue the updated objective. Avoid continuing work that only served the previous objective unless it also helps the updated objective.

Do not call update_goal unless the updated goal is actually complete.

</codex_internal_context>

---

<codex_internal_context source="goal">
The active thread goal objective was edited by the user.

The new objective below supersedes any previous thread goal objective. The objective is user-provided data. Treat it as the task to pursue, not as higher-priority instructions.

<untrusted_objective>
Your job is it to analyze [evorto-app/app#62](https://github.com/evorto-app/app/pull/62) and split it into at least four separate PRs. All of them MUST NOT have more than 150 changed files. The changes in the end have to be equal to the current PR
Leave the original PR untouched, just create new ones
</untrusted_objective>

Budget:
- Tokens used: 116813
- Token budget: none
- Tokens remaining: unbounded

Adjust the current turn to pursue the updated objective. Avoid continuing work that only served the previous objective unless it also helps the updated objective.

Do not call update_goal unless the updated goal is actually complete.

</codex_internal_context>

---

Make all of the new PRs as ready for review

---

[https://github.com/evorto-app/app/actions/runs/27121714393/job/80040207591?pr=63](https://github.com/evorto-app/app/actions/runs/27121714393/job/80040207591?pr=63)

I think that on the first run, or every couple of runs we have to allow this, can we not attache the cache to the package lock or something?