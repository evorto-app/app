Don't create new branches in this session, just work with me.
I dislike how many scripts we have in the [package.json](package.json) and how complex those scripts are. Explore what is going on, what these scripts are meant for and make suggestions about how to clean that up

---

Please do that, also I like your ideas for reducing the playwright scripts, make sure that we mention this in any relevant docs. I also think we can ignore the serve commands as that should be handled by the docker image, also when tying locally. Where do you want to move the scripts that should no longer stay in the package?
Some of them are still convenient for maintanance

---

Don't create a new branch for this, we just want to do some work on our project.
I think we should get rid of the docs directory in favor of context next to the code it is related to. Also make sure to keep a reference to that in local instructions files if you find a good place

Also please update our guidance to include a run of lint:fix before callind the webstorm tools for file issues.

Also I don't think I like the way you moved the commands into typescript, I would like to keep the important scripts in the package, see the full config of it and rather clean up and only keep the important parts to make it easy to understand and access

---

Read the following docs and prepare a solution for how we can massively simplify our env config while keeping the needed flexibility. I think we should be fine with -C dev. And then be able to name our files `.env` which is untracked and keeps actual secrets. `.env.dev` which includes non secret default config and `.env.dev.local` that includes only overrides for worktrees. In docker, we should not load the .env files but instead take needed things from the regular env and use the dotenv cli to load the files before executing docker commands. This should hopefully make our scripts much simpler and easier to understand
In CI we should make use of githubs options to configure the environment to our needs as the dotenv cli will not override those.

---

We should make .env.dev.local the tracked default config and .env.dev the generated override, so that it is not needed in the main directory

---

I think you fucked up the first wins implecations, Look at this comment again. The order still starts with .env
```dotenv-cli loads files **in order**, but only sets variables if they **haven’t already been set**.```

---

For others:

This is rather counter-intuitive, you wouldn't expect `FOO=1 FOO=2 sh -c 'echo $FOO'` to return `1`? - Rectifying this behaviour would be a significant breaking change.

To explain code with code, this `dotenv-cli` effectively does this:
```ts
const processEnv = { MY_VAR: "from_cli" };
const env1 = { MY_VAR: "from_env1" };
const env2 = { MY_VAR: "from_env2" };

const finalEnv = { ...env2, ...env1, ...processEnv };
console.log(finalEnv); // { MY_VAR: "from_cli" }
```

Running `MY_VAR=from_cli dotenv -e .env1 -e .env2 -- sh -c 'echo $MY_VAR'`

Will output: `from_cli`

Similarly without specifying `MY_VAR` in the cli, it will output `from_env1`

---

```
Some applications load environment variables from multiple `.env` files depending on the environment:

- `.env`
- `.env.local`
- `.env.development`
- `.env.development.local`

`dotenv-cli` supports this using the `-c` flag:

- `-c` loads `.env` and `.env.local`
- `-c test` loads `.env`, `.env.local`, `.env.test`, and `.env.test.local`

See [issue #37](https://github.com/dotenv-org/cli/issues/37) for more details.
```

---

I mean it's been 5 years and no one else has complained, but I spent about half an hour figuring out why I was connecting to my local database instead of my staging one 🙁

---

PLEASE IMPLEMENT THIS PLAN:
## Simplify Env Config with `dotenv -c dev` and Repo-Specific File Roles

### Summary

Adopt a simplified local env model based on `dotenv -c dev`, with repo-specific file roles chosen to match the actual `dotenv-cli` first-wins behavior:

- `.env`: untracked, developer secrets
- `.env.dev.local`: tracked, shared default dev config
- `.env.dev`: generated, untracked worktree override

Keep `bun run env:runtime` as the generator command, but change it to write `.env.dev`.

Remove dotenv loading from Docker Compose entirely. Local scripts preload env with `dotenv -c dev` before invoking `docker compose`. CI uses only GitHub Actions `env`, `vars`, and `secrets`.

### Important Behavior to Lock In

With `dotenv -c dev`, the effective dotenv file precedence is:

1. `.env.dev.local`
2. `.env.local`
3. `.env.dev`
4. `.env`

Because loading is first-wins, `.env.dev.local` beats `.env.dev`, and `.env.dev` beats `.env`.

For this repo, that means:

- `.env.dev.local` should hold tracked shared defaults.
- `.env.dev` should hold generated worktree-specific overrides.
- `.env.local` should be unsupported and documented as forbidden to avoid accidental precedence surprises.

### Key Changes

- **File contract**
  - Remove `.env.ci` and `.env.runtime` from the supported model.
  - Add tracked `.env.dev.local` with non-secret shared default config.
  - Keep `.env` untracked for secrets only.
  - Generate `.env.dev` for worktree-specific overrides.
  - Update `.gitignore` so `.env` and `.env.dev` stay untracked, while `.env.dev.local` is tracked.

- **Generated worktree overrides**
  - Keep `bun run env:runtime`.
  - Change `helpers/testing/runtime-env.ts` to write `.env.dev`.
  - Preserve the current generated worktree values there: `COMPOSE_PROJECT_NAME`, `APP_HOST_PORT`, `BASE_URL`, `DATABASE_URL`, `NEON_LOCAL_HOST_PORT`, `MINIO_HOST_PORT`, `MINIO_CONSOLE_HOST_PORT`, `DELETE_BRANCH`, `NEON_LOCAL_PROXY`, and related Neon-local values.
  - Document that the generator should be run on worktree creation and whenever local runtime settings need to be refreshed.

- **Scripts and Docker**
  - Simplify `package.json` scripts to use `dotenv -c dev` instead of the current repeated explicit `-e` chain.
  - Keep important scripts directly visible in `package.json`.
  - Update `test:e2e*`, `db:*`, and `docker:*` scripts to rely on the new cascade model.
  - Remove all `env_file` blocks from `docker-compose.yml`.
  - Keep Compose reading only normal environment variables supplied by the invoking shell.

- **Runtime/provider logic**
  - Update `src/server/config/provider.ts` so repo-local config precedence becomes:
    - real environment variables
    - `.env`
    - `.env.dev.local`
    - `.env.dev`
    - in-code defaults
  - Remove CI dotenv handling from the provider.
  - Update provider tests to reflect the new filenames and precedence.
  - Update `src/server/config/AGENTS.md` to explain the repo-specific file roles and to explicitly forbid `.env.local`.

- **CI**
  - Remove `.env.ci` from workflows and docs.
  - Configure all CI values through workflow/job `env`, GitHub `vars`, and GitHub `secrets`.
  - Keep local `dotenv -c dev` usage out of CI assumptions; CI already has exported env, and dotenv will not override it.

- **Docs and local instructions**
  - Update `tests/README.md`, `helpers/README.md`, root `README.md`, and relevant `AGENTS.md` files to describe:
    - `.env` as secrets
    - `.env.dev.local` as tracked shared defaults
    - `.env.dev` as generated worktree overrides
  - Keep testing/runtime context in `tests/README.md` and seeding/runtime-generation context in `helpers/README.md`.
  - Keep the instruction that `bun run lint:fix` must run before WebStorm `get_file_problems`.

### Public Surface / Contract Changes

- `bun run env:runtime` remains the command name, but now generates `.env.dev`.
- Supported local env files become:
  - `.env`
  - `.env.dev.local`
  - `.env.dev`
- Unsupported files in this repo model:
  - `.env.local`
  - `.env.runtime`
  - `.env.ci`
- Docker Compose no longer reads dotenv files directly.

### Test Plan

- Provider tests:
  - real env beats dotenv files
  - `.env.dev.local` beats `.env.dev`
  - `.env.dev` beats `.env`
  - missing `.env.dev` still works
- Script smoke tests:
  - `bun run test:e2e -- --help`
  - `bun run test:e2e:docs -- --help`
  - `bun run db:push -- --help`
  - `bun run docker:stop`
- Generator verification:
  - `bun run env:runtime` writes `.env.dev`
  - different worktrees still get distinct ports/project names
- CI verification:
  - e2e workflow still runs with GitHub-provided env only
- Final checks:
  - `bun run lint:fix`
  - targeted WebStorm `get_file_problems` on changed TS/config files

### Assumptions and Defaults

- Chosen approach: use plain `dotenv -c dev`.
- Chosen command name: keep `env:runtime`.
- Chosen file-role mapping:
  - `.env` = untracked secrets
  - `.env.dev.local` = tracked shared defaults
  - `.env.dev` = generated worktree overrides
- Chosen repo policy: `.env.local` is unsupported and should not be used.
- Chosen CI policy: no tracked CI dotenv file; GitHub Actions env is the only CI source of truth.

---

We should be able to get rid of the E2E_MODE variable and instead use the playwright projects. For the UI mode, there should be no restrictions as the UI allows selecting the correct tests to run.
Also explain why we have the sh commands in our scripts and if we can remove them from our scripts.

---

I would like to remove the --env-file flags from the docker commands, they should not be needed anymore

---

Why do we have zone.js installed?
The angular app should be zoneless

---

Please go ahead, commit first

---

As you can see in the changes I updated some packages, it seems that the build is no longer working now. Could you check whats up?

---

Here are the docs for drizzle with effect, maybe they can help you

Step 2 - Initialize the driver and make a query
Drizzle provides an Effect-native API that integrates with Effect’s service pattern. Use PgDrizzle.makeWithDefaults() to quickly create a Drizzle database instance with sensible defaults (no logging, no caching).

import 'dotenv/config';
import * as PgDrizzle from 'drizzle-orm/effect-postgres';
import { PgClient } from '@effect/sql-pg';
import * as Effect from 'effect/Effect';
import * as Redacted from 'effect/Redacted';
import { sql } from 'drizzle-orm';
import { types } from 'pg';
// Configure the PgClient layer with type parsers
const PgClientLive = PgClient.layer({
  url: Redacted.make(process.env.DATABASE_URL!),
  types: {
    getTypeParser: (typeId, format) => {
      // Return raw values for date/time types to let Drizzle handle parsing
      if ([1184, 1114, 1082, 1186, 1231, 1115, 1185, 1187, 1182].includes(typeId)) {
        return (val: any) => val;
      }
      return types.getTypeParser(typeId, format);
    },
  },
});
const program = Effect.gen(function*() {
  // Create the database with default services (no logging, no caching)
  const db = yield* PgDrizzle.makeWithDefaults();
  // Execute queries
  const result = yield* db.execute<{ id: number }>(sql`SELECT 1 as id`);
  console.log(result);
});
// Run the program with the PgClient layer
Effect.runPromise(program.pipe(Effect.provide(PgClientLive)));

Step 3 - Create a DB Layer for dependency injection
For larger applications, create a reusable DB layer that can be composed with other services. This follows Effect’s recommended pattern for dependency injection:

import * as PgDrizzle from 'drizzle-orm/effect-postgres';
import { PgClient } from '@effect/sql-pg';
import * as Context from 'effect/Context';
import * as Effect from 'effect/Effect';
import * as Layer from 'effect/Layer';
import * as Redacted from 'effect/Redacted';
import { types } from 'pg';
import * as relations from './schema/relations';
// Configure the PgClient layer
const PgClientLive = PgClient.layer({
  url: Redacted.make(process.env.DATABASE_URL!),
  types: {
    getTypeParser: (typeId, format) => {
      if ([1184, 1114, 1082, 1186, 1231, 1115, 1185, 1187, 1182].includes(typeId)) {
        return (val: any) => val;
      }
      return types.getTypeParser(typeId, format);
    },
  },
});
// Create the DB effect with default services
const dbEffect = PgDrizzle.make({ relations }).pipe(
  Effect.provide(PgDrizzle.DefaultServices)
);
// Define a DB service tag for dependency injection
class DB extends Context.Tag('DB')<DB, Effect.Effect.Success<typeof dbEffect>>() {}
// Create a layer that provides the DB service
const DBLive = Layer.effect(
  DB,
  Effect.gen(function*() {
    return yield* dbEffect;
  }),
);
// Compose all layers together
const AppLive = Layer.provideMerge(DBLive, PgClientLive);
// Use the DB service in your application
const program = Effect.gen(function*() {
  const db = yield* DB;
  const users = yield* db.select().from(usersTable);
  return users;
});
// Run with all dependencies provided
Effect.runPromise(program.pipe(Effect.provide(AppLive)));

Advanced: Custom logger and cache configuration
For more control over logging and caching, use PgDrizzle.make() directly and provide your own service implementations.

Logger configuration
By default, makeWithDefaults() uses a no-op logger (no logging). You can enable logging by providing a different EffectLogger implementation:

import * as PgDrizzle from 'drizzle-orm/effect-postgres';
import { EffectLogger } from 'drizzle-orm/effect-postgres';
import * as Effect from 'effect/Effect';
const program = Effect.gen(function*() {
  const db = yield* PgDrizzle.make({ /* schema, relations, casing */ }).pipe(
    // Enable Effect-based logging (uses Effect.log with annotations)
    Effect.provide(EffectLogger.layer),
    // Provide remaining default services (cache)
    Effect.provide(PgDrizzle.DefaultServices),
  );
  const users = yield* db.select().from(usersTable);
  return users;
});

Available logger options:

EffectLogger.Default - No-op logger (no logging occurs) - this is the default
EffectLogger.layer - Logs queries using Effect’s Effect.log() with annotations for query SQL and parameters. Integrates with Effect’s logging infrastructure.
EffectLogger.fromDrizzle(logger) - Wraps a Drizzle Logger instance for use with Effect
EffectLogger.layerFromDrizzle(logger) - Creates an Effect Layer from a Drizzle logger
When using EffectLogger.layer, queries are logged via Effect’s logging system. You can configure the output format by providing a different Effect logger layer (e.g., Logger.pretty for development, Logger.json for production).

Using a Drizzle logger:

import * as PgDrizzle from 'drizzle-orm/effect-postgres';
import { EffectLogger } from 'drizzle-orm/effect-postgres';
import * as Effect from 'effect/Effect';
import { DefaultLogger } from 'drizzle-orm';
const program = Effect.gen(function*() {
  const db = yield* PgDrizzle.make({ /* schema, relations, casing */ }).pipe(
    // Use a Drizzle logger wrapped for Effect
    Effect.provide(EffectLogger.layerFromDrizzle(new DefaultLogger())),
    // Provide remaining default services (cache)
    Effect.provide(PgDrizzle.DefaultServices),
  );
  const users = yield* db.select().from(usersTable);
  return users;
});

Cache configuration
Similarly, you can provide a custom cache implementation:

import * as PgDrizzle from 'drizzle-orm/effect-postgres';
import { EffectLogger } from 'drizzle-orm/effect-postgres';
import { EffectCache } from 'drizzle-orm/cache/core/cache-effect';
import * as Effect from 'effect/Effect';
import { MyCustomCache } from './cache';
const program = Effect.gen(function*() {
  const db = yield* PgDrizzle.make({ /* schema, relations, casing */ }).pipe(
    // Provide a custom cache wrapped for Effect
    Effect.provide(EffectCache.layerFromDrizzle(new MyCustomCache())),
    // Provide remaining default services (logger)
    Effect.provide(PgDrizzle.DefaultServices),
  );
  const users = yield* db.select().from(usersTable);
  return users;
});

Available cache options:

EffectCache.Default - No-op cache (no caching occurs) - this is the default
EffectCache.fromDrizzle(cache) - Wraps a Drizzle Cache instance for use with Effect
EffectCache.layerFromDrizzle(cache) - Creates an Effect Layer from a Drizzle cache (useful for composing with other layers)

---

Please commit

---

Please commit