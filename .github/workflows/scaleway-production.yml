name: Promote Scaleway production

on:
  workflow_dispatch:
    inputs:
      staging_manifest_key:
        description: Immutable successful staging manifest object key
        required: true
        type: string
      confirmation:
        description: Type promote-alpha to request protected-environment approval
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: scaleway-production-deployment
  cancel-in-progress: false

jobs:
  promote:
    name: Promote accepted staging digest
    if: vars.PRODUCTION_ENABLED == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    environment: scaleway-production
    env:
      SCW_DEFAULT_ORGANIZATION_ID: ${{ vars.SCW_ORGANIZATION_ID }}
      SCW_DEFAULT_PROJECT_ID: ${{ vars.SCW_PROJECT_ID }}
      SCW_DEFAULT_REGION: fr-par
      SCW_DEFAULT_ZONE: fr-par-1
      TF_IN_AUTOMATION: "true"
      TF_INPUT: "false"
      TF_VAR_alert_email: ${{ vars.ALERT_EMAIL }}
      TF_VAR_bucket_suffix: ${{ vars.BUCKET_SUFFIX }}
      TF_VAR_cloudflare_zone_id: ${{ vars.CLOUDFLARE_ZONE_ID }}
      TF_VAR_deployer_application_id: ${{ vars.SCW_DEPLOYER_APPLICATION_ID }}
      TF_VAR_project_id: ${{ vars.SCW_PROJECT_ID }}
      TF_VAR_runtime_database_password_version: ${{ vars.PRODUCTION_RUNTIME_DATABASE_PASSWORD_VERSION }}
      TF_VAR_schema_database_password_version: ${{ vars.PRODUCTION_SCHEMA_DATABASE_PASSWORD_VERSION }}
      TF_VAR_tem_project_id: ${{ vars.SCW_TEM_PROJECT_ID }}
      TF_VAR_web_application_id: ${{ vars.SCW_WEB_APPLICATION_ID }}
      TF_VAR_worker_application_id: ${{ vars.SCW_WORKER_APPLICATION_ID }}
    steps:
      - name: Validate explicit production enablement and protected configuration
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.PRODUCTION_TERRAFORM_STATE_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.PRODUCTION_TERRAFORM_STATE_SECRET_ACCESS_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CONFIRMATION: ${{ inputs.confirmation }}
          PROTECTED_SECRET_VALUES: ${{ secrets.ROLE_SECRET_VALUES_JSON }}
          RUNTIME_DATABASE_PASSWORD: ${{ secrets.PRODUCTION_RUNTIME_DATABASE_PASSWORD }}
          SCHEMA_DATABASE_PASSWORD: ${{ secrets.PRODUCTION_SCHEMA_DATABASE_PASSWORD }}
          SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
          SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
          PRODUCTION_TERRAFORM_STATE_BUCKET: ${{ vars.PRODUCTION_TERRAFORM_STATE_BUCKET }}
        run: |
          if [ "${CONFIRMATION}" != "promote-alpha" ]; then
            echo "::error::Production promotion confirmation did not match promote-alpha"
            exit 1
          fi
          for variable_name in \
            AWS_ACCESS_KEY_ID \
            AWS_SECRET_ACCESS_KEY \
            CLOUDFLARE_API_TOKEN \
            PROTECTED_SECRET_VALUES \
            RUNTIME_DATABASE_PASSWORD \
            SCHEMA_DATABASE_PASSWORD \
            SCW_ACCESS_KEY \
            SCW_DEFAULT_ORGANIZATION_ID \
            SCW_DEFAULT_PROJECT_ID \
            SCW_SECRET_KEY \
            PRODUCTION_TERRAFORM_STATE_BUCKET \
            TF_VAR_alert_email \
            TF_VAR_bucket_suffix \
            TF_VAR_cloudflare_zone_id \
            TF_VAR_deployer_application_id \
            TF_VAR_project_id \
            TF_VAR_runtime_database_password_version \
            TF_VAR_schema_database_password_version \
            TF_VAR_tem_project_id \
            TF_VAR_web_application_id \
            TF_VAR_worker_application_id; do
            if [ -z "${!variable_name}" ]; then
              echo "::error::Missing required protected production configuration: ${variable_name}"
              exit 1
            fi
          done
          command -v aws >/dev/null
          jq --exit-status 'type == "object"' <<<"${PROTECTED_SECRET_VALUES}" >/dev/null

      - name: Fetch and validate the accepted staging manifest
        id: staging
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.SCW_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SCW_SECRET_KEY }}
          MANIFEST_KEY: ${{ inputs.staging_manifest_key }}
          STAGING_METADATA_BUCKET: evorto-staging-deployment-${{ vars.BUCKET_SUFFIX }}
        run: |
          if [[ ! "${MANIFEST_KEY}" =~ ^deployments/staging/[0-9a-f]{40}/[0-9]+-[0-9]+\.json$ ]]; then
            echo "::error::Production accepts only immutable staging deployment manifest keys"
            exit 1
          fi
          mkdir -p deployment
          aws s3 cp \
            "s3://${STAGING_METADATA_BUCKET}/${MANIFEST_KEY}" \
            deployment/staging-manifest.json \
            --endpoint-url https://s3.fr-par.scw.cloud \
            --only-show-errors
          jq --exit-status \
            '.status == "succeeded"
              and .environment == "staging"
              and (.revision | test("^[0-9a-f]{40}$"))
              and (.digest | test("^sha256:[0-9a-f]{64}$"))
              and (.schemaHash | test("^[0-9a-f]{64}$"))
              and (.image | startswith("rg.fr-par.scw.cloud/evorto-staging/evorto@sha256:"))
              and (.sourceMapsKey | test("^source-maps/[0-9a-f]{40}/[0-9a-f]{64}\\.tar\\.gz$"))
              and (.sbomKey | test("^sbom/[0-9a-f]{40}/[0-9a-f]{64}\\.spdx\\.json$"))' \
            deployment/staging-manifest.json >/dev/null

          revision="$(jq --raw-output .revision deployment/staging-manifest.json)"
          digest="$(jq --raw-output .digest deployment/staging-manifest.json)"
          image="$(jq --raw-output .image deployment/staging-manifest.json)"
          schema_hash="$(jq --raw-output .schemaHash deployment/staging-manifest.json)"
          {
            echo "digest=${digest}"
            echo "image=${image}"
            echo "revision=${revision}"
            echo "schema_hash=${schema_hash}"
          } >> "${GITHUB_OUTPUT}"

      - name: Checkout the exact accepted staging revision
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          clean: false
          persist-credentials: false
          ref: ${{ steps.staging.outputs.revision }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: 1.15.8
          terraform_wrapper: false

      - name: Install checksum-verified Scaleway CLI
        run: |
          ops/scaleway/install-cli.sh "${RUNNER_TEMP}/scw"
          echo "SCW_CLI=${RUNNER_TEMP}/scw" >> "${GITHUB_ENV}"

      - name: Initialize remote state
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.PRODUCTION_TERRAFORM_STATE_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.PRODUCTION_TERRAFORM_STATE_SECRET_ACCESS_KEY }}
          PRODUCTION_TERRAFORM_STATE_BUCKET: ${{ vars.PRODUCTION_TERRAFORM_STATE_BUCKET }}
        run: |
          terraform -chdir=infrastructure/scaleway/production init \
            -backend-config="bucket=${PRODUCTION_TERRAFORM_STATE_BUCKET}" \
            -input=false \
            -reconfigure

      - name: Login to private Scaleway registry
        env:
          REGISTRY_PASSWORD: ${{ secrets.SCW_SECRET_KEY }}
        run: |
          printf '%s' "${REGISTRY_PASSWORD}" \
            | docker login rg.fr-par.scw.cloud --username nologin --password-stdin

      - name: Copy the accepted digest without rebuilding
        id: image
        env:
          SOURCE_DIGEST: ${{ steps.staging.outputs.digest }}
          SOURCE_IMAGE: ${{ steps.staging.outputs.image }}
          REVISION: ${{ steps.staging.outputs.revision }}
        run: |
          target_tag="rg.fr-par.scw.cloud/evorto-production/evorto:sha-${REVISION}"
          docker buildx imagetools create \
            --tag "${target_tag}" \
            "${SOURCE_IMAGE}"
          target_digest="sha256:$(
            docker buildx imagetools inspect --raw "${target_tag}" \
              | sha256sum \
              | awk '{print $1}'
          )"
          if [ "${target_digest}" != "${SOURCE_DIGEST}" ]; then
            echo "::error::Production registry copy changed the accepted image digest"
            exit 1
          fi

          target_reference="rg.fr-par.scw.cloud/evorto-production/evorto@${target_digest}"
          docker pull --platform linux/amd64 "${target_reference}"
          bash ops/scaleway/verify-runtime-image.sh "${target_reference}"
          schema_container_id="$(
            docker create \
              --platform linux/amd64 \
              "${target_reference}"
          )"
          packaged_schema_file="${RUNNER_TEMP}/packaged-schema.mjs"
          docker cp \
            "${schema_container_id}:/app/dist/evorto/ops/schema.mjs" \
            "${packaged_schema_file}"
          docker rm "${schema_container_id}" >/dev/null
          packaged_schema_hash="$(
            sha256sum "${packaged_schema_file}" | awk '{print $1}'
          )"
          rm -f "${packaged_schema_file}"
          if [ "${packaged_schema_hash}" != "${{ steps.staging.outputs.schema_hash }}" ]; then
            echo "::error::Production image schema hash differs from the staging manifest"
            exit 1
          fi
          {
            echo "digest=${target_digest}"
            echo "reference=${target_reference}"
          } >> "${GITHUB_OUTPUT}"

      - name: Scan exact promoted image digest
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ steps.image.outputs.reference }}
          version: v0.70.0
          format: table
          severity: HIGH,CRITICAL
          exit-code: "1"
          hide-progress: true

      - name: Verify production infrastructure has no pending changes
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.PRODUCTION_TERRAFORM_STATE_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.PRODUCTION_TERRAFORM_STATE_SECRET_ACCESS_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
          SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
          TF_VAR_container_image: ${{ steps.image.outputs.reference }}
          TF_VAR_runtime_database_password: ${{ secrets.PRODUCTION_RUNTIME_DATABASE_PASSWORD }}
          TF_VAR_schema_database_password: ${{ secrets.PRODUCTION_SCHEMA_DATABASE_PASSWORD }}
        run: |
          set +e
          terraform -chdir=infrastructure/scaleway/production plan \
            -detailed-exitcode \
            -input=false
          plan_status="$?"
          set -e
          if [ "${plan_status}" -eq 2 ]; then
            echo "::error::Production infrastructure has pending changes. Apply them explicitly, review the result, then rerun this forward deployment."
            exit 1
          fi
          if [ "${plan_status}" -ne 0 ]; then
            exit "${plan_status}"
          fi
          terraform -chdir=infrastructure/scaleway/production output -json platform > deployment/platform.json
          terraform -chdir=infrastructure/scaleway/production output -json database > deployment/database.json

      - name: Reconcile production role-scoped Secret Manager values
        env:
          PROTECTED_SECRET_VALUES: ${{ secrets.ROLE_SECRET_VALUES_JSON }}
          RUNTIME_DATABASE_PASSWORD: ${{ secrets.PRODUCTION_RUNTIME_DATABASE_PASSWORD }}
          SCHEMA_DATABASE_PASSWORD: ${{ secrets.PRODUCTION_SCHEMA_DATABASE_PASSWORD }}
          SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
          SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
        run: |
          umask 077
          printf '%s' "${PROTECTED_SECRET_VALUES}" > deployment/protected-secret-values.json
          ops/scaleway/sync-secret-values.sh \
            production \
            deployment/platform.json \
            deployment/database.json \
            deployment/protected-secret-values.json
          rm -f deployment/protected-secret-values.json deployment/database.json

      - name: Copy private source maps and SBOM from accepted staging evidence
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.SCW_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SCW_SECRET_KEY }}
          STAGING_METADATA_BUCKET: evorto-staging-deployment-${{ vars.BUCKET_SUFFIX }}
        run: |
          source_maps_key="$(jq --raw-output .sourceMapsKey deployment/staging-manifest.json)"
          sbom_key="$(jq --raw-output .sbomKey deployment/staging-manifest.json)"
          production_bucket="$(jq --raw-output .deployment_metadata_bucket deployment/platform.json)"
          mkdir -p deployment/evidence
          aws s3 cp \
            "s3://${STAGING_METADATA_BUCKET}/${source_maps_key}" \
            deployment/evidence/source-maps.tar.gz \
            --endpoint-url https://s3.fr-par.scw.cloud \
            --only-show-errors
          aws s3 cp \
            "s3://${STAGING_METADATA_BUCKET}/${sbom_key}" \
            deployment/evidence/sbom.spdx.json \
            --endpoint-url https://s3.fr-par.scw.cloud \
            --only-show-errors
          aws s3 cp \
            deployment/evidence/source-maps.tar.gz \
            "s3://${production_bucket}/${source_maps_key}" \
            --endpoint-url https://s3.fr-par.scw.cloud \
            --only-show-errors
          aws s3 cp \
            deployment/evidence/sbom.spdx.json \
            "s3://${production_bucket}/${sbom_key}" \
            --endpoint-url https://s3.fr-par.scw.cloud \
            --only-show-errors

      - name: Deploy production ops and apply only a stable safe schema plan
        env:
          DIGEST: ${{ steps.image.outputs.digest }}
          IMAGE_REFERENCE: ${{ steps.image.outputs.reference }}
          REVISION: ${{ steps.staging.outputs.revision }}
          SCHEMA_HASH: ${{ steps.staging.outputs.schema_hash }}
          SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
          SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
        run: |
          ops/scaleway/deploy-role.sh \
            deployment/platform.json ops \
            "${IMAGE_REFERENCE}" "${REVISION}" "${DIGEST}" "${SCHEMA_HASH}"

          ops_endpoint="$(jq --raw-output .containers.ops.endpoint deployment/platform.json)"
          explain_response="$(
            ops/scaleway/invoke-private-container.sh \
              "${ops_endpoint}" \
              /internal/ops/schema-explain \
              '{}'
          )"
          jq --exit-status \
            --arg schema_hash "${SCHEMA_HASH}" \
            '.safe == true and .schemaHash == $schema_hash' \
            <<<"${explain_response}" >/dev/null
          plan_digest="$(jq --raw-output .digest <<<"${explain_response}")"
          apply_response="$(
            ops/scaleway/invoke-private-container.sh \
              "${ops_endpoint}" \
              /internal/ops/schema-apply \
              "$(jq --null-input --compact-output --arg plan_digest "${plan_digest}" '{ planDigest: $plan_digest }')"
          )"
          jq --exit-status \
            --arg plan_digest "${plan_digest}" \
            '.applied == true and .digest == $plan_digest' \
            <<<"${apply_response}" >/dev/null

      - name: Deploy production worker and web at the accepted digest
        env:
          DIGEST: ${{ steps.image.outputs.digest }}
          IMAGE_REFERENCE: ${{ steps.image.outputs.reference }}
          REVISION: ${{ steps.staging.outputs.revision }}
          SCHEMA_HASH: ${{ steps.staging.outputs.schema_hash }}
          SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
          SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
        run: |
          ops/scaleway/deploy-role.sh \
            deployment/platform.json worker \
            "${IMAGE_REFERENCE}" "${REVISION}" "${DIGEST}" "${SCHEMA_HASH}"
          ops/scaleway/deploy-role.sh \
            deployment/platform.json web \
            "${IMAGE_REFERENCE}" "${REVISION}" "${DIGEST}" "${SCHEMA_HASH}"

      - name: Smoke the promoted production release
        env:
          DIGEST: ${{ steps.image.outputs.digest }}
          REVISION: ${{ steps.staging.outputs.revision }}
        run: |
          curl_args=(--connect-timeout 5 --max-time 20 --silent --show-error)

          health_body="$(curl "${curl_args[@]}" --fail https://alpha.evorto.app/healthz)"
          jq --exit-status '.status == "ok"' <<<"${health_body}" >/dev/null
          ready_status="$(curl "${curl_args[@]}" --output /dev/null --write-out '%{http_code}' https://alpha.evorto.app/readyz)"
          test "${ready_status}" = "204"
          version_body="$(curl "${curl_args[@]}" --fail https://alpha.evorto.app/version)"
          jq --exit-status \
            --arg revision "${REVISION}" \
            --arg digest "${DIGEST}" \
            '.environment == "production" and .revision == $revision and .imageDigest == $digest' \
            <<<"${version_body}" >/dev/null
          curl "${curl_args[@]}" --fail \
            --header 'X-Forwarded-Host: attacker.invalid' \
            https://alpha.evorto.app/events \
            | grep --quiet '<app-root'
          login_status="$(curl "${curl_args[@]}" --output /dev/null --write-out '%{http_code}' https://alpha.evorto.app/login)"
          [[ "${login_status}" =~ ^30[12378]$ ]]
          generated_endpoint="$(jq --raw-output .containers.web.endpoint deployment/platform.json)"
          generated_status="$(curl "${curl_args[@]}" --output /dev/null --write-out '%{http_code}' "${generated_endpoint}")"
          test "${generated_status}" = "404"

      - name: Write append-only production deployment manifest
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.SCW_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SCW_SECRET_KEY }}
          DIGEST: ${{ steps.image.outputs.digest }}
          IMAGE_REFERENCE: ${{ steps.image.outputs.reference }}
          REVISION: ${{ steps.staging.outputs.revision }}
          SCHEMA_HASH: ${{ steps.staging.outputs.schema_hash }}
          SOURCE_STAGING_MANIFEST_KEY: ${{ inputs.staging_manifest_key }}
        run: |
          timestamp="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          source_maps_key="$(jq --raw-output .sourceMapsKey deployment/staging-manifest.json)"
          sbom_key="$(jq --raw-output .sbomKey deployment/staging-manifest.json)"
          manifest_key="deployments/production/${REVISION}/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}.json"
          jq --null-input \
            --arg digest "${DIGEST}" \
            --arg image "${IMAGE_REFERENCE}" \
            --arg revision "${REVISION}" \
            --arg sbom_key "${sbom_key}" \
            --arg schema_hash "${SCHEMA_HASH}" \
            --arg source_maps_key "${source_maps_key}" \
            --arg source_staging_manifest_key "${SOURCE_STAGING_MANIFEST_KEY}" \
            --arg timestamp "${timestamp}" \
            --arg workflow "${GITHUB_WORKFLOW}" \
            --arg workflow_run_id "${GITHUB_RUN_ID}" \
            --arg workflow_run_attempt "${GITHUB_RUN_ATTEMPT}" \
            '{
              status: "succeeded",
              environment: "production",
              revision: $revision,
              image: $image,
              digest: $digest,
              schemaHash: $schema_hash,
              sourceMapsKey: $source_maps_key,
              sbomKey: $sbom_key,
              sourceStagingManifestKey: $source_staging_manifest_key,
              workflow: $workflow,
              workflowRunId: $workflow_run_id,
              workflowRunAttempt: $workflow_run_attempt,
              timestamp: $timestamp
            }' > deployment/manifest.json
          metadata_bucket="$(jq --raw-output .deployment_metadata_bucket deployment/platform.json)"
          aws s3api put-object \
            --bucket "${metadata_bucket}" \
            --key "${manifest_key}" \
            --body deployment/manifest.json \
            --endpoint-url https://s3.fr-par.scw.cloud \
            --if-none-match '*' \
            >/dev/null
          aws s3api put-object \
            --bucket "${metadata_bucket}" \
            --key deployments/production/latest.json \
            --body deployment/manifest.json \
            --endpoint-url https://s3.fr-par.scw.cloud \
            >/dev/null

      - name: Upload production deployment evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: scaleway-production-${{ steps.staging.outputs.revision }}
          path: deployment/manifest.json
          if-no-files-found: error
          retention-days: 90
