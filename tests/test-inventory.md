# Playwright Test Inventory (New)

Scope: New Playwright coverage under `tests/**`. Legacy coverage remains in `e2e/tests/test-inventory.md`.

## Documentation journeys (`*.doc.ts`)

- TBD

## Functional suites (`*.test.ts` / `*.spec.ts`)

- TBD

## Notes

- Add new tests here and tag them by feature (`@finance`, `@templates`, etc.).
