import { test } from '../../support/fixtures/parallel-test';

test.describe('Inclusive price labels', () => {
  test.fixme('paid prices display inclusive tax labels', async () => {});

  test.fixme('free prices do not show tax labels', async () => {});

  test.fixme('zero percent tax rate shows "Tax free" label', async () => {});

  test.fixme('fallback label shown when tax rate unavailable', async () => {});

  test.fixme('price labels consistent across all pages', async () => {});

  test.fixme('discounted prices maintain inclusive tax labels', async () => {});

  test.fixme('template detail view shows inclusive labels for paid options', async () => {});
});
