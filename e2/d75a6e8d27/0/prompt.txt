I was tightening the generated documentation source guards in PR #62.

Just pushed:
- `c1349114` `Guard generated doc screenshot aliases`
- It prevents docs tests from bypassing the shared screenshot helper by destructuring `screenshot` or `attach` into local aliases.
- Verified with focused source tests, format, lint, `git diff --check`, and WebStorm errors-only diagnostics.

Current state:
- PR head: `c134911406ffbda7f5bc1428c5c9e9209b05296d`
- Worktree clean and aligned with origin
- Non-E2E checks green
- E2E run `27070739168` still running, currently at cache warmer
- Superseded E2E run was cancelled
- Neon dry-run clean: only protected `main`

Planned next:
- Wait/check whether E2E on `c1349114` passes or fails.
- If it fails, inspect logs and fix.
- If it keeps running/green, continue the stabilization pass by finding the next concrete evidence gap in `STABILIZATION.md`, especially around docs image quality, viewport/mobile General coverage, and Browser-verifiable UI evidence.