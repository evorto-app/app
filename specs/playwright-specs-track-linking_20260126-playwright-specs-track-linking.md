# Playwright Specs + Track Linking

Conductor Track: conductor/tracks/playwright-specs-track-linking_20260126/spec.md

## Requirements covered

- req-playwright-config: Run Playwright tests from the root `tests/` directory.
- req-specs-template: Provide a track-linked spec template in `specs/`.
- req-tags: Require `@track(...)` plus `@req(...)`/`@doc(...)` tags in new tests.
- req-tag-check: Fail lint when required tags are missing in `tests/**`.

## Tests implementing this spec

- TBD

## Doc tests implementing this spec

- TBD

## Notes

- Legacy tests remain in `e2e/tests/**` for reference only.
